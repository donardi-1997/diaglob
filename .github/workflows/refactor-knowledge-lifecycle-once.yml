name: One-off Knowledge Provisioning Lifecycle Refactor

on:
  push:
    branches:
      - refactor/knowledge-provisioning-lifecycle
    paths:
      - .github/workflows/refactor-knowledge-lifecycle-once.yml

permissions:
  contents: write

jobs:
  refactor:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Extract transactional lifecycle helpers
        run: python3 scripts/refactor_knowledge_lifecycle_once.py
      - name: Commit refactor
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend/app/bedrock_knowledge_base.py
          git diff --cached --quiet && exit 0
          git commit -m "refactor: delegate provisioning lifecycle state"
          git push origin HEAD:${GITHUB_REF_NAME}
