name: One-off Bedrock Resource Adapter Refactor

on:
  push:
    branches:
      - refactor/knowledge-bedrock-resource-adapter
    paths:
      - .github/workflows/refactor-bedrock-resources-once.yml

permissions:
  contents: write

jobs:
  refactor:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Delegate Bedrock resource operations
        run: python3 scripts/refactor_bedrock_resources_once.py
      - name: Commit refactor
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend/app/bedrock_knowledge_base.py
          git diff --cached --quiet && exit 0
          git commit -m "refactor: delegate Bedrock resource operations"
          git push origin HEAD:${GITHUB_REF_NAME}
